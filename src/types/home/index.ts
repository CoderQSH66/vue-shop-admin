export interface ITagsType {
  name: string
  path: string
}

export type ITimeType = 'month' | 'week' | 'hour'

export interface IPayData {
  subTitle: string
  subUnit: string
  subValue: number
  title: string
  unit: string
  unitColor: string
  value: number
}
